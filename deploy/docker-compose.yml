services:
  postgresql:
    image: postgis/postgis:16-3.4
    container_name: pet-postgresql
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-pet_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-pet_password}
      POSTGRES_DB: ${POSTGRES_DB:-pet_platform}
    ports:
      - "5432:5432"
    volumes:
      - pet_postgresql_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: pet-redis
    ports:
      - "6379:6379"

  minio:
    image: minio/minio:RELEASE.2025-09-07T16-13-09Z
    container_name: pet-minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-minioadmin}
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - pet_minio_data:/data

  backend:
    build:
      context: ..
      dockerfile: backend/pet-nestjs/Dockerfile
    container_name: pet-backend
    depends_on:
      - postgresql
      - redis
      - minio
    environment:
      NODE_ENV: production
      PORT: 3000
      DATABASE_URL: postgresql://${POSTGRES_USER:-pet_user}:${POSTGRES_PASSWORD:-pet_password}@postgresql:5432/${POSTGRES_DB:-pet_platform}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      JWT_SECRET: ${JWT_SECRET:-dev_only_replace_before_production}
      JWT_EXPIRES_IN: ${JWT_EXPIRES_IN:-7d}
      STORAGE_DRIVER: minio
      MINIO_ENDPOINT: minio
      MINIO_PORT: 9000
      MINIO_USE_SSL: "false"
      MINIO_ACCESS_KEY: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_SECRET_KEY: ${MINIO_ROOT_PASSWORD:-minioadmin}
      MINIO_BUCKET: ${MINIO_BUCKET:-pet-platform}
      PUBLIC_FILE_BASE_URL: ${PUBLIC_FILE_BASE_URL:-http://localhost:9000/pet-platform}
      UPLOAD_MAX_BYTES: ${UPLOAD_MAX_BYTES:-10485760}
    ports:
      - "3000:3000"

  nginx:
    image: nginx:1.25-alpine
    container_name: pet-nginx
    depends_on:
      - backend
    ports:
      - "8080:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/pet-platform.conf:/etc/nginx/conf.d/pet-platform.conf:ro
      - ../frontend/pet-uniapp/dist/build/h5:/usr/share/nginx/html/h5:ro
      - ../admin/pet-admin/dist:/usr/share/nginx/html/admin:ro

volumes:
  pet_postgresql_data:
  pet_minio_data:
