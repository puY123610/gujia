# 项目进度

## 当前阶段

阶段：可交接开发骨架。

日期：2026-05-05。

## 已完成

- 项目定位、技术栈、登录策略和权限分层文档已建立；
- 根目录 pnpm workspace 已建立；
- 三端工程目录已建立；
- 后端 NestJS 基础入口、权限装饰器和守卫骨架已建立；
- 用户端 uni-app 基础入口、登录守卫和请求封装骨架已建立；
- 管理端 Vue3 基础入口已建立；
- Docker Compose 已覆盖 PostgreSQL/PostGIS、Redis、MinIO、后端和 Nginx；
- OpenAPI 首版接口契约已建立；
- Postman 集合基线已建立；
- 环境检查、初始化、类型生成、服务启动、停止、部署、备份脚本已建立。
- GitHub 远程仓库已推送到 `puY123610/gujia`；
- 协作分支已建立：`main`、`dev`、`feat/frontend-pet-uniapp`、`feat/backend-pet-nestjs`；
- 当前约定：前端分支 PR 到 `dev`，后端分支 PR 到 `dev`，联调通过后 `dev` PR 到 `main`。

## 当前未完成

- 未安装依赖，未生成 lockfile；
- 未实现真实业务接口；
- 未实现数据库业务模型和迁移；
- 未实现 JWT 登录、刷新和退出；
- 未实现上传文件到 MinIO / OSS / COS；
- 未实现前端页面业务；
- 未实现管理后台业务；
- 未接入测试和 CI。

## 下一步

1. 前端在 `feat/frontend-pet-uniapp` 上实现首页、急救知识、医院列表、寻宠列表和登录引导。
2. 后端在 `feat/backend-pet-nestjs` 上实现 Auth 模块、JWT 解析管道和 Prisma 核心模型。
3. 前后端共同维护 `api-contract/openapi.yaml` 和 `docs/接口文档.md`。
4. 功能完成后分别 PR 到 `dev` 联调。
5. `dev` 在线测试通过后再 PR 到 `main`。

## 风险和约束

- 用户端不能启动即强制登录；
- Public 接口不能错误加登录守卫；
- UserAuth / AdminAuth 不能直接相信前端 userId；
- 文件本体不能直接存数据库；
- 数据库结构变更必须通过迁移管理；
- 敏感配置必须使用环境变量。
